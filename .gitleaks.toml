title = "Priv-Eye gitleaks config"

[allowlist]
description = "Allowlist for known non-secret values committed intentionally"

regexes = [
  "ci-test-secret-not-real-at-least-32-chars",
  "training-only-not-for-production-use-padding-x",
  "replace_with_openssl_rand_64",
]

paths = [
  ".env.example",
]
