title = "Priv-Eye gitleaks config"

[allowlist]
description = "Allowlist for known test/CI values"
regexes = [
  # CI test JWT secret — not a real secret
  "ci-test-secret-not-real-at-least-32-chars",
  # Training-only placeholder
  "training-only-not-for-production-use-padding-x",
]
